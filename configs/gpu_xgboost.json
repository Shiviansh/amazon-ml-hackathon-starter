{
  "engine": "xgboost",
  "task": "regression",
  "learning_rate": 0.03,
  "max_iterations": 2000,
  "early_stopping_rounds": 100,
  "device": "gpu",
  "model_params": {
    "tree_method": "hist",
    "device": "cuda",
    "max_depth": 8,
    "subsample": 0.8,
    "colsample_bytree": 0.7,
    "reg_alpha": 0.1,
    "reg_lambda": 1.0
  }
}
